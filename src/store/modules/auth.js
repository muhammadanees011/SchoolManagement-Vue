// ----------Auth Module----------
const state = {
  user: localStorage.getItem('user'),
  accessToken: localStorage.getItem('token'),
}
const mutations = {}
const actions = {}
const getters = {}

export default {
  state,
  mutations,
  actions,
  getters,
}
